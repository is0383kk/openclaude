{
  "env": {
    // Add environment variables here. These will be available to the agent when it runs.
  },
  "permissions": {},
  "maxTurns": 20,
  "autoMemoryEnabled": false // OpenClaude is currently in early development and does not support memory features.
}
